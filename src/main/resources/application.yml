server:
  servlet:
    encoding:
      force: true
    
spring:
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/ingram?serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useSSL=false
    username: pyh
    password: pyh1234
    
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update #서버 재실행해도 데이터 유지 // create, update, none(실제 production 모드에서 사용)
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB

  security:
    user:
      name: test
      password: 1234   

#file:
#  path: C:/workspace/springbootwork/upload/
